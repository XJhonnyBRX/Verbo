# =============================================================================
# VERBO
#
# Este arquivo é versionado de propósito: ele não guarda segredo nenhum.
# Segredos e chaves vão em .env.local, que está no .gitignore.
# =============================================================================

# Tradução ativa. Precisa existir em bible_translations, importada por
# `npm run import:bible`. Ver docs/design/traducao.md.
NEXT_PUBLIC_VERBO_TRANSLATION=blivre
